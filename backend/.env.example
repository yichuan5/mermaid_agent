# OpenAI-compatible API Configuration
OPENAI_API_KEY=

# Optional: Override the base URL for custom endpoints (e.g. Ollama, Azure, Together, etc.)
OPENAI_BASE_URL=

# Optional: Override the model name
OPENAI_MODEL=